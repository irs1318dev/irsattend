"""Sync attendance data with the main student roster."""

import pathlib


def add_student_ids_to_roster(roster_config_path: pathlib.Path) -> None:
    """Add student IDs to roster."""


def sync_attendnce_datawith_roster(roster_config_path: pathlib.Path) -> None:
    """Synchronize attendance data with the roster."""
